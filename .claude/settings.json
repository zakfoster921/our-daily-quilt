{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ \"$f\" == */lib/simplified-quilt-app-boot.js ]] && cp \"$f\" \"$(dirname \"$f\")/../ios/App/App/public/lib/simplified-quilt-app-boot.js\" 2>/dev/null || true; } || true"
          }
        ]
      }
    ]
  }
}

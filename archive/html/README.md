# Legacy & lab HTML

Non-production HTML moved out of the repo root during Phase 9 cleanup.

**Production app (do not move):** `our-daily-beta.html`, `index.html`, `privacy.html`, `support.html` at repo root.

Open lab files via local static server from repo root, e.g. `npx serve -l 3456 .` then `/archive/html/mood-triptych-lab.html`.

Excluded from Docker (`.dockerignore`).
